import React, { Component } from 'react';
import { WriterWrapper } from '../style';

class Write extends Component {
  render() {
    return (
      <WriterWrapper>Write</WriterWrapper>
    )
  }
}

export default Write;